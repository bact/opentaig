site/
__pycache__/
*.pyc
.DS_Store
.python-version

# Regenerated per curation run from a fresh site/data.json (see
# curation/export_rq_context.py) -- a transient input, not source.
curation/rq_context.json

# Regenerated per curation run by dedup_candidates.py (derivable from
# search_candidates.csv + seen_repos.csv + site/data.json) -- not source.
curation/state/candidates_to_review.csv

# Regenerated on demand by emit_harm_framework.py (fully derivable from
# harm_taxonomy_mapping.py's MAPPING data + rq_context.json) -- pasteable
# output, not source. Re-run the script if the harms framework's live-sheet
# rows ever need re-pasting.
curation/candidate_framework_harms.csv
curation/candidate_terms_harms.csv
curation/candidate_map_harms.csv
